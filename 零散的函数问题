plt.subplot(numrows, numcols, fignum)


没有axis参数表示全部相加，axis＝0表示按列相加，axis＝1表示按照行的方向相加


tf.assign(
    ref,
    value,
    validate_shape=None,
    use_locking=None,
    name=None
)
返回ref


anaconda快捷安装tensorflow
https://anaconda.org/conda-forge/tensorflow 
conda install -c conda-forge tensorflow=1.1.0



http://www.cnblogs.com/pinard/p/6251584.html
奇异值分解(SVD)原理与在降维中的应用


tensorboard --inspect --logdir=/path          检测是否存在文件
localhost:6006                              在chrome输入到tensorboard

tf.scalar_summary----->tf.summary.scalar()  版本更新

tf.equal(A, B)是对比这两个矩阵或者向量的相等的元素，如果是相等的那就返回True，反正返回False，返回的值的矩阵维度和A是

decode的作用是将其他编码的字符串转换成unicode编码，如str1.decode('gb2312')，表示将gb2312编码的字符串str1转换成unicode编码。 

encode的作用是将unicode编码转换成其他编码的字符串，如str2.encode('gb2312')，表示将unicode编码的字符串str2转换成gb2312编码。 
os.path.join（path，* paths）
智能地加入一个或多个路径组件

os.getcwd() 方法用于返回当前工作目录。

conv2d(
    input,
    filter,
    strides,
    padding,
    use_cudnn_on_gpu=None,
    data_format=None,
    name=None
)计算给定4-D input和filter张量的2-D卷积。

tf.nn.max_pool
max_pool(
    value,
    ksize,
    strides,
    padding,
    data_format='NHWC',
    name=None
)在输入上执行最大池数。

tf.nn.relu

relu(
    features,
    name=None
)计算线性整流


tf.nn.softmax_cross_entropy_with_logits

softmax_cross_entropy_with_logits(
    _sentinel=None,
    labels=None,
    logits=None,
    dim=-1,
    name=None
)
Defined in tensorflow/python/ops/nn_ops.py.

See the guide: Neural Network > Classification

Computes softmax cross entropy between logits and labels.

tf.argmax

tf.argmax

argmax(
    input,
    axis=None,
    name=None,
    dimension=None
)


permute(A,[2 1 3]);%例如A是4×5×6的三维数组，permute后A变成5×4×6


# 'x' is [1, 4]
# 'y' is [2, 5]
# 'z' is [3, 6]
pack([x, y, z]) => [[1, 4], [2, 5], [3, 6]]  # Pack along first dim.
pack([x, y, z], axis=1) => [[1, 2, 3], [4, 5, 6]]


http://www.jianshu.com/p/fb119d0ff6a6
Tensorflow的softmax_cross_entropy_with_logits函数


该类实现一个简单的机制来协调一组线程的终止
tf.train.Coordinator()

tf.train.Coordinator.should_stop: returns True if the threads should stop.
tf.train.Coordinator.request_stop: requests that threads should stop.
tf.train.Coordinator.join: waits until the specified threads have stopped.

time.sleep(t) 进程推迟t秒
